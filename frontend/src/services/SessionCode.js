import React, { useEffect, useState } from "react";


function SessionCode() {
  // React State
//   const [count, setCount] = useState(null);

//   useEffect(() => {
//     // Access count value from session storage
//     if (cartView == null) {
//       // Initialize page views count
//       cartView = 1;
//     } else {
//       // Increment count
//       cartView = Number(cartView) + 1;
//     }
//     // Update session storage
//     sessionStorage.setItem("cartView", cartView);
//     setCount(cartView)
//   }, []); //No dependency to trigger in each page load

//   return (
//     <div className="app">
//       <div>Cart View Count is:</div>
//       {count}
//     </div>
    
//   );
}
export default SessionCode;

